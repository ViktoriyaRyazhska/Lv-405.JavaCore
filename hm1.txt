import java.io.IOException;
import java.io.BufferedReader;
import java.io.InputStreamReader;

public class Main {

    public static void main(String[] args) throws IOException {
        
        // Home work !

       // 1

        /*int radius;
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        System.out.println("Enter radius:");
        radius = Integer.parseInt(br.readLine());
        System.out.println("Perimetr = " + 2 * radius * Math.PI);
        System.out.println("Area = " + Math.PI * radius * radius);*/


       // 2
        
        /*String name;
        String live;
        String adress;

        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        System.out.println("What is your name!?");
        name = br.readLine();
        System.out.println("Where do you live?");
        live = br.readLine();
        System.out.println("What is your adress?");
        adress = br.readLine();*/

        // 3
        
        /*int t1;
        int t2;
        int t3;
        int tar1 = 5;
        int tar2 = 10;
        int tar3 = 15;
        String c1;
        String c2;
        String c3;

        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        System.out.println("Enter to console  time(minutes) and countries please:");
        System.out.println("Enter minutes:");
        t1 = Integer.parseInt(br.readLine());
        System.out.println("Enter country:");
        c1 = br.readLine();
        System.out.println("Tarif  call 1 minute cost " + tar1 + "$ to " + c1 );
        System.out.println("Enter second time(minute) and country:");
        System.out.println("Enter minutes:");
        t2 = Integer.parseInt(br.readLine());
        System.out.println("Enter country:");
        c2 = br.readLine();
        System.out.println("Tarif call 1 minute cost " + tar2 + "$ to " + c2 );
        System.out.println("Enter third time(minute) and country:");
        System.out.println("Enter minutes:");
        t3 = Integer.parseInt(br.readLine());
        System.out.println("Enter country:");
        c3 = br.readLine();
        System.out.println("Tarif call 1 minute cost " + tar3 + "$ to " + c3 );
        System.out.println();
        System.out.println("First call with " + c1 + " continued: " + t1 + " minutes. It cost " + (tar1 * t1));
        System.out.println("Second call with " + c2 + " continued: " + t2 + " minutes. It cost " + (tar2 * t2));
        System.out.println("Third call with " + c3 + " continued: " + t3 + " minutes. It cost " + (tar3 * t3));
        System.out.println();
        System.out.println("All talk together continued: "+ (t1+t2+t3) + " minutes. It cost " + (tar1*t1+tar2*t2+tar3*t3)+ "$.");*/